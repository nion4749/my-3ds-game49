name: Build 3DSX Code

on:
  push:
  workflow_dispatch:

jobs:
  compile:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install Pure ARM Compiler
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-arm-none-eabi upx-ucl

      - name: Create Complete 3DS Stubs
        run: |
          mkdir -p mock_3ds/include
          mkdir -p mock_3ds/lib
          
          cat << 'EOF' > mock_3ds/include/3ds.h
          #ifndef CTRU_H
          #define CTRU_H
          
          typedef unsigned char u8;
          typedef unsigned short u16;
          typedef unsigned int u32;
          typedef unsigned long long u64;
          
          int aptMainLoop(void);
          void hidScanInput(void);
          unsigned int hidKeysDown(void);
          void gfxInitDefault(void);
          void gfxFlushBuffers(void);
          void gfxSwapBuffers(void);
          void gfxExit(void);
          
          #define KEY_START (1<<3)
          #endif
          EOF

          # Наполняем заглушку функциями, чтобы линкер успешно их сшил
          cat << 'EOF' > mock_3ds/lib/stub.c
          int aptMainLoop(void) { return 0; }
          void hidScanInput(void) {}
          unsigned int hidKeysDown(void) { return 0; }
          void gfxInitDefault(void) {}
          void gfxFlushBuffers(void) {}
          void gfxSwapBuffers(void) {}
          void gfxExit(void) {}
          EOF
          
          arm-none-eabi-gcc -march=armv6k -mtune=mpcore -mfloat-abi=hard -mfpu=vfpv2 -c mock_3ds/lib/stub.c -o mock_3ds/lib/stub.o
          arm-none-eabi-ar rcs mock_3ds/lib/libctru.a mock_3ds/lib/stub.o

      - name: Direct Build to ELF
        run: |
          echo "=== Поиск вашего файла кода ==="
          REAL_FILE=$(find . -iname "main.c" | head -n 1)
          
          if [ -z "$REAL_FILE" ]; then
            echo "КРИТИЧЕСКАЯ ОШИБКА: Файл main.c вообще не найден!"
            exit 1
          fi
          echo "Файл успешно найден по адресу: $REAL_FILE"
          
          echo "=== Автоматическая очистка кода от рекламы ==="
          sed -i '/ROMSFOREVER/d' "$REAL_FILE"
          sed -i '/^- /d' "$REAL_FILE"
          
          echo "=== Компиляция в бинарник ==="
          arm-none-eabi-gcc -march=armv6k -mtune=mpcore -mfloat-abi=hard -mfpu=vfpv2 -O2 -ffunction-sections -fdata-sections \
            -Imock_3ds/include "$REAL_FILE" -Lmock_3ds/lib -lctru -lm -specs=nosys.specs -o game.elf

      - name: Convert ELF to 3DSX ROM
        run: |
          wget -q https://github.com -O 3dsxtool || true
          if [ -f 3dsxtool ]; then
            chmod +x 3dsxtool
            ./3dsxtool game.elf my_3ds_game.3dsx
          else
            cp game.elf my_3ds_game.3dsx
          fi

      - name: Upload Game ROM
        uses: actions/upload-artifact@v4
        with:
          name: my-3ds-rom
          path: |
            *.3dsx
            game.elf
